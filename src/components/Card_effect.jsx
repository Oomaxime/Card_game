const Card_effect = ({ effect }) => {
  return (
    <div className="card-container">
      <ul className="card-list">
        <li className="cardItem">{effect}</li>
      </ul>
    </div>
  );
};

export default Card_effect;
